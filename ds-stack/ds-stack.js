var Stack = function() {
    this._storage = []

    this.add = function(){
      // write me
      //add elements to the _storage.
      this._storage.push('1','5') //concat()?
      //this._storage.push.concat('1','5')
      // return this._storage

    };
    this.remove = function() {
      // write me
      //remove elements from the _storage.
      this._storage.unshift()

      // return 

    };
  };
